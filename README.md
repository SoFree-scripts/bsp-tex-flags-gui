# Graphical User Interface to dump out the surface texture flags compiled into a .bsp map file.
Takes input : ` * ` to dump all textures' flags.
Or a substring to match against the texture relative path.